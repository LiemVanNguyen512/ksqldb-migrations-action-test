CREATE STREAM TASKS_STREAM (
    payload STRUCT<
        before STRUCT<
            id BIGINT,
            parent_id BIGINT,
            party_id INT,
            party_name STRING,
            external_id STRING,
            search_code STRING,
            "type" STRING,
            status STRING,
            sub_status STRING,
            sla_start_date STRING,
            sla_end_date STRING,
            sla_warn_level STRING,
            plan_start_date STRING,
            wish_date STRING,
            plan_complete_date STRING,
            task_body STRING,
            reason_code STRING,
            reason_text STRING,
            branch STRING,
            created_at STRING,
            created_by INT,
            updated_at STRING,
            updated_by INT,
            revision INT,
            org_id STRING,
            attachments STRING,
            comments STRING,
            category STRING,
            priority STRING,
            completed BOOLEAN,
            coordinates STRING,
            ac_info STRING,
            dhid STRING,
            version STRING,
            deadlines STRING,
            group_id BIGINT,
            delivered_at STRING,
            party_code STRING,
            network_owner_code STRING,
            closed_at STRING,
            area_code STRING,
            sub_type STRING,
            is_created_from_dynamic_workflow BOOLEAN,
            system_id STRING
        >,
        after STRUCT<
            id BIGINT,
            parent_id BIGINT,
            party_id INT,
            party_name STRING,
            external_id STRING,
            search_code STRING,
            type STRING,
            status STRING,
            sub_status STRING,
            sla_start_date STRING,
            sla_end_date STRING,
            sla_warn_level STRING,
            plan_start_date STRING,
            wish_date STRING,
            plan_complete_date STRING,
            task_body STRING,
            reason_code STRING,
            reason_text STRING,
            branch STRING,
            created_at STRING,
            created_by INT,
            updated_at STRING,
            updated_by INT,
            revision INT,
            org_id STRING,
            attachments STRING,
            comments STRING,
            category STRING,
            priority STRING,
            completed BOOLEAN,
            coordinates STRING,
            ac_info STRING,
            dhid STRING,
            version STRING,
            deadlines STRING,
            group_id BIGINT,
            delivered_at STRING,
            party_code STRING,
            network_owner_code STRING,
            closed_at STRING,
            area_code STRING,
            sub_type STRING,
            is_created_from_dynamic_workflow BOOLEAN,
            system_id STRING
        >,
        source STRUCT<
            version STRING,
            "connector" STRING,
            name STRING,
            ts_ms BIGINT,
            snapshot STRING,
            db STRING,
            sequence STRING,
            schema STRING,
            "table" STRING,
            txId BIGINT,
            lsn BIGINT,
            xmin BIGINT
        >,
        op STRING,
        ts_ms BIGINT
    >
) WITH (
    KAFKA_TOPIC = 'gridsz_task_dev.task.tasks',
    VALUE_FORMAT = 'JSON'
);